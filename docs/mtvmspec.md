# mt Virtual Machine Code Specification

## Types

- `#` Numerical constant

## Commands

- `push` push adds a value to the accumulator
- `hlt` hlt stops execution
- `add` adds the top two numbers on the stack
